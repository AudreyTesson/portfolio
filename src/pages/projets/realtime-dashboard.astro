---
import Layout from '../../layouts/Layout.astro';
import ProjectNavigation from '../../components/ProjectNavigation.astro';

// Code content - Using variables to avoid template literal parsing issues
const configYamlExample = `# config/packages/realtime_dashboard.yaml
realtime_dashboard:
  widgets:
    orders_today:
      type: counter
      entity: App\\Entity\\Order
      query: "createdAt >= :today"
      refresh: 5s

    revenue_chart:
      type: line_chart
      source: App\\Dashboard\\RevenueSource
      refresh: 30s`;

const composerExample = `composer require realtime-dashboard/bundle`;
---

<Layout
  title="Realtime Dashboard — Template Symfony SSE | Audrey Tesson"
  description="Template de dashboard temps reel pour Symfony avec SSE. Widgets prets a l'emploi, zero configuration, UI moderne."
  activePage="projets"
>
  <div class="project-page">

    <!-- 1. HERO SECTION (2 colonnes) -->
    <section id="hero" class="hero-section">
      <div class="container">
        <div class="hero-grid">
          <!-- Left: Content -->
          <div class="hero-content">
            <!-- Badge contextuel -->
            <div class="context-badge">
              <span class="badge-type">Template</span>
              <span class="badge-separator">·</span>
              <span class="badge-role">Solo</span>
              <span class="badge-separator">·</span>
              <span class="badge-status">Disponible</span>
            </div>

            <h1>Realtime Dashboard</h1>
            <p class="tagline">Dashboard temps reel pour votre application Symfony</p>
            <p class="subtitle">Visualisez vos donnees en temps reel grace a SSE — Zero configuration, UI moderne incluse</p>

            <!-- Metrics -->
            <div class="hero-metrics">
              <div class="metric">
                <span class="metric-value">&lt;100ms</span>
                <span class="metric-label">latence</span>
              </div>
              <div class="metric">
                <span class="metric-value">0</span>
                <span class="metric-label">dependance externe</span>
              </div>
              <div class="metric">
                <span class="metric-value">10min</span>
                <span class="metric-label">pour demarrer</span>
              </div>
            </div>

            <!-- CTAs -->
            <div class="hero-ctas">
              <a href="#demo" class="btn btn-primary">Voir la demo</a>
              <a href="https://github.com" class="btn btn-secondary">Documentation</a>
            </div>

            <!-- Pricing inline -->
            <div class="hero-pricing">
              <span class="pricing-value">29EUR</span>
              <span class="pricing-label">one-time · Mises a jour 1 an</span>
            </div>

            <!-- Author mention -->
            <p class="author-mention">Concu et developpe par <a href="/about">Audrey Tesson</a></p>
          </div>

          <!-- Right: Hero Mockup -->
          <div class="hero-visual">
            <div class="hero-mockup">
              <div class="mockup-badge">PLACEHOLDER</div>
              <!-- Mockup: Dashboard avec widgets temps reel -->
              <div class="mockup-content">
                <div class="mockup-header">
                  <div class="mockup-nav">
                    <span class="nav-item active">Overview</span>
                    <span class="nav-item">Analytics</span>
                    <span class="nav-item">Alerts</span>
                  </div>
                  <div class="mockup-live-badge">
                    <span class="live-dot"></span>
                    LIVE
                  </div>
                </div>
                <div class="mockup-widgets">
                  <div class="widget-counter">
                    <span class="widget-label">Commandes</span>
                    <span class="widget-value">127</span>
                    <span class="widget-trend up">+12%</span>
                  </div>
                  <div class="widget-counter">
                    <span class="widget-label">Revenus</span>
                    <span class="widget-value">4,250EUR</span>
                    <span class="widget-trend up">+8%</span>
                  </div>
                  <div class="widget-chart">
                    <div class="chart-placeholder">
                      <div class="chart-bar" style="height: 40%"></div>
                      <div class="chart-bar" style="height: 60%"></div>
                      <div class="chart-bar" style="height: 45%"></div>
                      <div class="chart-bar" style="height: 80%"></div>
                      <div class="chart-bar" style="height: 65%"></div>
                      <div class="chart-bar active" style="height: 90%"></div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <p class="visual-note">* Remplacer par screenshot reel du dashboard</p>
          </div>
        </div>
      </div>
    </section>

    <!-- 2. TL;DR - Resume Express -->
    <section class="tldr-section">
      <div class="container">
        <div class="tldr-grid">
          <div class="tldr-item">
            <span class="tldr-icon">🎯</span>
            <span class="tldr-label">Objectif</span>
            <span class="tldr-value">Ajouter des dashboards temps reel a Symfony</span>
          </div>
          <div class="tldr-item">
            <span class="tldr-icon">👥</span>
            <span class="tldr-label">Cible</span>
            <span class="tldr-value">Developpeurs Symfony, Startups</span>
          </div>
          <div class="tldr-item">
            <span class="tldr-icon">👤</span>
            <span class="tldr-label">Mon role</span>
            <span class="tldr-value">Solo — Template cle en main</span>
          </div>
          <div class="tldr-item">
            <span class="tldr-icon">⚙️</span>
            <span class="tldr-label">Stack</span>
            <span class="tldr-value">Symfony 7 · SSE · Stimulus</span>
            <span class="tldr-subvalue">Sans WebSocket externe</span>
          </div>
          <div class="tldr-item">
            <span class="tldr-icon">📊</span>
            <span class="tldr-label">Resultat</span>
            <span class="tldr-value">Dashboard live en 10 minutes</span>
          </div>
        </div>
      </div>
    </section>

    <!-- 3. PROBLEME / SOLUTION -->
    <section id="solution" class="problem-section">
      <div class="container">
        <!-- 3.1 Le Constat -->
        <div class="problem-header">
          <span class="section-tag">Le constat</span>
          <h2>Implementer du temps reel dans Symfony, c'est penible</h2>
        </div>

        <div class="pain-points">
          <div class="pain-point">
            <span class="pain-icon">⏱️</span>
            <div class="pain-content">
              <strong>3 jours</strong>
              <span>pour implementer SSE from scratch</span>
            </div>
          </div>
          <div class="pain-point">
            <span class="pain-icon">🔧</span>
            <div class="pain-content">
              <strong>Config complexe</strong>
              <span>Mercure, WebSockets, infra...</span>
            </div>
          </div>
          <div class="pain-point">
            <span class="pain-icon">📊</span>
            <div class="pain-content">
              <strong>UI basique</strong>
              <span>Grafana/Metabase = overkill</span>
            </div>
          </div>
        </div>

        <!-- 3.2 La Transformation -->
        <div class="transformation-section">
          <div class="transformation-header">
            <span class="section-tag">La solution</span>
            <h3>Un dashboard temps reel en 10 minutes</h3>
          </div>

          <div class="split-visual">
            <!-- Before -->
            <div class="split-before">
              <div class="split-label">Avant</div>
              <div class="code-preview">
                <div class="code-lines">
                  <div class="code-line comment">// 200+ lignes de config...</div>
                  <div class="code-line">StreamedResponse</div>
                  <div class="code-line">EventSource</div>
                  <div class="code-line">setInterval</div>
                  <div class="code-line comment">// Widgets custom...</div>
                  <div class="code-line">chart.js init</div>
                  <div class="code-line error">// Bugs de reconnexion</div>
                </div>
              </div>
              <p class="split-caption">Code custom eparpille</p>
            </div>

            <!-- Transform center -->
            <div class="split-center">
              <div class="transform-animation">
                <div class="transform-icon">📦</div>
                <div class="transform-text">
                  <span>composer require</span>
                  <span class="transform-time">~10min</span>
                </div>
              </div>
            </div>

            <!-- After -->
            <div class="split-after">
              <div class="split-label success">Apres</div>
              <div class="dashboard-preview">
                <div class="dp-header">Dashboard</div>
                <div class="dp-widgets">
                  <div class="dp-widget"></div>
                  <div class="dp-widget"></div>
                  <div class="dp-widget large"></div>
                </div>
                <div class="dp-status">
                  <span class="dp-live"></span>
                  Live
                </div>
              </div>
              <p class="split-caption success">Dashboard fonctionnel</p>
            </div>
          </div>

          <!-- Results -->
          <div class="split-features">
            <div class="split-feature">
              <span class="feature-check">✓</span>
              <span>SSE natif Symfony</span>
            </div>
            <div class="split-feature">
              <span class="feature-check">✓</span>
              <span>Widgets prets a l'emploi</span>
            </div>
            <div class="split-feature">
              <span class="feature-check">✓</span>
              <span>Zero infrastructure externe</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 4. DEMO / FONCTIONNALITES -->
    <section id="demo" class="demo-section">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">Comment ca marche</span>
          <h2>3 etapes, 10 minutes</h2>
        </div>

        <div class="steps-timeline">
          <div class="step">
            <div class="step-number">1</div>
            <div class="step-icon">📦</div>
            <h4>Installation</h4>
            <p>Une commande Composer, configuration auto via Flex</p>
            <div class="step-code">composer require</div>
          </div>

          <div class="step-arrow">→</div>

          <div class="step highlight">
            <div class="step-number">2</div>
            <div class="step-icon">⚙️</div>
            <h4>Configuration</h4>
            <p>Definissez vos widgets en YAML</p>
            <div class="step-tech">
              <span class="tech-badge">YAML</span>
              <span class="tech-badge">Doctrine</span>
            </div>
          </div>

          <div class="step-arrow">→</div>

          <div class="step">
            <div class="step-number">3</div>
            <div class="step-icon">🚀</div>
            <h4>C'est pret !</h4>
            <p>Dashboard temps reel fonctionnel</p>
            <div class="step-ready">/admin/dashboard</div>
          </div>
        </div>

        <!-- Code example -->
        <div class="code-example">
          <div class="code-header">
            <span class="code-dot red"></span>
            <span class="code-dot yellow"></span>
            <span class="code-dot green"></span>
            <span class="code-title">config/packages/realtime_dashboard.yaml</span>
          </div>
          <pre class="code-content" set:text={configYamlExample}></pre>
        </div>
      </div>
    </section>

    <!-- 5. CAS D'USAGE -->
    <section id="usecases" class="usecases-section">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">Cas d'usage</span>
          <h2>Adapte a vos besoins</h2>
        </div>

        <div class="usecases-grid">
          <div class="usecase-card">
            <div class="usecase-icon">📈</div>
            <div class="usecase-content">
              <h4>Admin Dashboard</h4>
              <p>Metriques business en temps reel : commandes, revenus, utilisateurs actifs.</p>
              <div class="usecase-metrics">
                <div class="usecase-metric">
                  <span class="metric-value">-90%</span>
                  <span class="metric-label">temps dev</span>
                </div>
                <div class="usecase-metric">
                  <span class="metric-value">100%</span>
                  <span class="metric-label">temps reel</span>
                </div>
              </div>
            </div>
            <div class="usecase-target">Ideal pour : SaaS, E-commerce</div>
          </div>

          <div class="usecase-card">
            <div class="usecase-icon">🖥️</div>
            <div class="usecase-content">
              <h4>Monitoring interne</h4>
              <p>Suivi des jobs, queues, erreurs en live. Detectez les problemes instantanement.</p>
              <div class="usecase-metrics">
                <div class="usecase-metric">
                  <span class="metric-value">&lt;100ms</span>
                  <span class="metric-label">latence</span>
                </div>
                <div class="usecase-metric">
                  <span class="metric-value">0</span>
                  <span class="metric-label">refresh manuel</span>
                </div>
              </div>
            </div>
            <div class="usecase-target">Ideal pour : DevOps, Operations</div>
          </div>

          <div class="usecase-card">
            <div class="usecase-icon">📊</div>
            <div class="usecase-content">
              <h4>Reporting client</h4>
              <p>Dashboards embarques pour vos clients B2B. White-label et multi-tenant ready.</p>
              <div class="usecase-metrics">
                <div class="usecase-metric">
                  <span class="metric-value">100%</span>
                  <span class="metric-label">white-label</span>
                </div>
                <div class="usecase-metric">
                  <span class="metric-value">Multi</span>
                  <span class="metric-label">tenant</span>
                </div>
              </div>
            </div>
            <div class="usecase-target">Ideal pour : Agences, Editeurs SaaS</div>
          </div>

          <div class="usecase-card">
            <div class="usecase-icon">🎓</div>
            <div class="usecase-content">
              <h4>Apprentissage SSE</h4>
              <p>Code source documente pour comprendre SSE dans Symfony. 100% natif, zero magie.</p>
              <div class="usecase-metrics">
                <div class="usecase-metric">
                  <span class="metric-value">100%</span>
                  <span class="metric-label">documente</span>
                </div>
                <div class="usecase-metric">
                  <span class="metric-value">PHPStan 8</span>
                  <span class="metric-label">qualite</span>
                </div>
              </div>
            </div>
            <div class="usecase-target">Ideal pour : Developpeurs, Formateurs</div>
          </div>
        </div>
      </div>
    </section>

    <!-- 6. PREUVE & TRACTION -->
    <section class="proof-section">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">Ce qui est inclus</span>
          <h2>Template complet et documente</h2>
        </div>

        <div class="proof-grid">
          <div class="proof-item">
            <span class="proof-value">5+</span>
            <span class="proof-label">widgets inclus</span>
          </div>
          <div class="proof-item">
            <span class="proof-value">&lt;100ms</span>
            <span class="proof-label">latence SSE</span>
          </div>
          <div class="proof-item">
            <span class="proof-value">0</span>
            <span class="proof-label">dependance externe</span>
          </div>
          <div class="proof-item">
            <span class="proof-value">85%</span>
            <span class="proof-label">couverture tests</span>
          </div>
        </div>

        <!-- Testimonial -->
        <div class="testimonial">
          <blockquote>
            "Enfin un template SSE propre pour Symfony. J'ai pu ajouter un dashboard temps reel a mon SaaS en une apres-midi."
          </blockquote>
          <cite>— Developpeur Symfony freelance</cite>
        </div>
      </div>
    </section>

    <!-- 6.5 MINI CTA FREELANCE -->
    <section class="mini-cta-section">
      <div class="container">
        <div class="mini-cta-banner">
          <span class="mini-cta-icon">💼</span>
          <span class="mini-cta-text">Besoin d'un dashboard sur-mesure ?</span>
          <a href="/contact" class="mini-cta-btn">Me contacter →</a>
        </div>
      </div>
    </section>

    <!-- 7. TECH & ARCHITECTURE -->
    <section id="tech" class="tech-section">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">Sous le capot</span>
          <h2>Architecture & Stack technique</h2>
        </div>

        <!-- Stack badges en ligne -->
        <div class="stack-badges">
          <span class="stack-badge">PHP 8.4</span>
          <span class="stack-badge">Symfony 7</span>
          <span class="stack-badge">Doctrine</span>
          <span class="stack-badge">SSE natif</span>
          <span class="stack-badge">Stimulus</span>
          <span class="stack-badge">Turbo</span>
          <span class="stack-badge">TailwindCSS</span>
        </div>

        <!-- Architecture diagram (text boxes style from mockup-v2) -->
        <div class="architecture">
          <h3>Architecture simplifiee</h3>
          <div class="arch-diagram">
            <div class="arch-node">
              <span class="arch-icon">🌐</span>
              <span>Browser</span>
            </div>
            <div class="arch-arrow">→</div>
            <div class="arch-node">
              <span class="arch-icon">🎮</span>
              <span>Controller</span>
            </div>
            <div class="arch-arrow">→</div>
            <div class="arch-node highlight">
              <span class="arch-icon">📡</span>
              <span>SSE Stream</span>
            </div>
            <div class="arch-arrow">→</div>
            <div class="arch-node">
              <span class="arch-icon">📊</span>
              <span>Dashboard UI</span>
            </div>
          </div>
          <div class="arch-sub">
            <div class="arch-sub-flow">
              <span class="arch-sub-node">Doctrine Events</span>
              <span class="arch-sub-arrow">→</span>
              <span class="arch-sub-node">Event Dispatcher</span>
              <span class="arch-sub-arrow">→</span>
              <span class="arch-sub-node">SSE Push</span>
            </div>
          </div>
        </div>

        <!-- Technical decisions -->
        <div class="tech-decisions">
          <h3>Decisions techniques</h3>
          <div class="decisions-grid">
            <div class="decision">
              <h4>Pourquoi SSE natif ?</h4>
              <p>Zero infrastructure externe (pas de Mercure/Redis obligatoire). Fonctionne out-of-the-box sur tout hebergement PHP.</p>
            </div>
            <div class="decision">
              <h4>Pourquoi Stimulus ?</h4>
              <p>Integration native avec Symfony UX. Reactive sans framework JS lourd. Parfait pour les widgets interactifs.</p>
            </div>
            <div class="decision">
              <h4>Pourquoi pas WebSocket ?</h4>
              <p>SSE suffit pour 90% des cas (donnees serveur → client). Plus simple a deployer, pas de serveur supplementaire.</p>
            </div>
          </div>
        </div>

        <!-- Challenges -->
        <div class="challenges">
          <h3>Challenges & Solutions</h3>
          <div class="challenges-table">
            <div class="challenge-row">
              <div class="challenge">
                <span class="challenge-icon">⚠️</span>
                <span>Keep-alive connections qui timeout</span>
              </div>
              <div class="solution">
                <span class="solution-icon">✓</span>
                <span>Gestion auto des timeouts + reconnexion client</span>
              </div>
            </div>
            <div class="challenge-row">
              <div class="challenge">
                <span class="challenge-icon">⚠️</span>
                <span>Performance avec beaucoup de widgets</span>
              </div>
              <div class="solution">
                <span class="solution-icon">✓</span>
                <span>Batching des events + throttling configurable</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 8. ROLE & PROCESS -->
    <section class="role-section">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">Mon role</span>
          <h2>Ce que j'ai fait sur ce projet</h2>
        </div>

        <div class="role-intro">
          <div class="role-badge-box">
            <span class="rb-icon">👤</span>
            <div class="rb-info">
              <strong>Projet solo</strong>
              <span>Template cle en main</span>
            </div>
          </div>
        </div>

        <div class="skills-columns">
          <div class="skill-column">
            <div class="sc-header">
              <span class="sc-icon">🎨</span>
              <h4>Product</h4>
            </div>
            <ul class="sc-list">
              <li>Definition des widgets standards</li>
              <li>UX dashboard intuitive</li>
              <li>Documentation complete</li>
              <li>Exemples de configuration</li>
            </ul>
          </div>

          <div class="skill-column">
            <div class="sc-header">
              <span class="sc-icon">⚙️</span>
              <h4>Backend</h4>
            </div>
            <ul class="sc-list">
              <li>SSE Symfony natif</li>
              <li>Event system complet</li>
              <li>Configuration YAML</li>
              <li>Integration Doctrine</li>
            </ul>
          </div>

          <div class="skill-column">
            <div class="sc-header">
              <span class="sc-icon">🖥️</span>
              <h4>Frontend</h4>
            </div>
            <ul class="sc-list">
              <li>Widgets Stimulus</li>
              <li>Animations CSS fluides</li>
              <li>Theming personnalisable</li>
              <li>Responsive design</li>
            </ul>
          </div>

          <div class="skill-column">
            <div class="sc-header">
              <span class="sc-icon">📊</span>
              <h4>Qualite</h4>
            </div>
            <ul class="sc-list">
              <li>Tests PHPUnit (85%)</li>
              <li>PHPStan level 8</li>
              <li>Demo fonctionnelle</li>
              <li>Documentation API</li>
            </ul>
          </div>
        </div>

        <div class="methodology-bar">
          <span class="mb-label">Approche :</span>
          <span class="mb-item">Documentation-first</span>
          <span class="mb-sep">·</span>
          <span class="mb-item">Feedback early adopters</span>
          <span class="mb-sep">·</span>
          <span class="mb-item">Code propre</span>
        </div>
      </div>
    </section>

    <!-- 9. DUAL CTA -->
    <section id="contact" class="cta-section">
      <div class="container">
        <div class="dual-cta">
          <div class="cta-product">
            <h3>Obtenir Realtime Dashboard</h3>
            <p>Template complet avec widgets, documentation et support</p>
            <div class="cta-price">
              <span class="price-value">29EUR</span>
              <span class="price-period">one-time</span>
            </div>
            <a href="#" class="btn btn-primary">Acheter maintenant</a>
            <a href="#demo" class="btn-link">Voir la demo →</a>
          </div>

          <div class="cta-divider">
            <span>ou</span>
          </div>

          <div class="cta-freelance">
            <h3>Dashboard sur-mesure ?</h3>
            <p>Besoin d'un dashboard temps reel personnalise ou d'une integration complexe ?</p>
            <div class="cta-specialization">
              Specialisee Symfony, temps reel, SSE/WebSocket
            </div>
            <a href="/contact" class="btn btn-secondary">Discutons de votre projet</a>
            <p class="cta-subtext">Disponible pour missions freelance</p>
          </div>
        </div>
      </div>
    </section>

  </div>

  <ProjectNavigation currentSlug="realtime-dashboard" />
</Layout>

<style>
  /* ===== GENERAL ===== */
  .project-page {
    padding-top: 80px;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .section-tag {
    display: inline-block;
    background: rgba(139, 92, 246, 0.1);
    color: var(--color-primary);
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 1rem;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-header h2 {
    font-size: 2rem;
    font-weight: 700;
    margin: 0;
  }

  /* ===== HERO SECTION (2 colonnes) ===== */
  .hero-section {
    padding: 6rem 0 4rem;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-content {
    text-align: left;
  }

  .context-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(139, 92, 246, 0.1);
    padding: 0.5rem 1rem;
    border-radius: 30px;
    margin-bottom: 1.5rem;
    font-size: 0.85rem;
  }

  .badge-type {
    color: var(--color-primary);
    font-weight: 600;
  }

  .badge-separator {
    color: rgba(255, 255, 255, 0.3);
  }

  .badge-role,
  .badge-status {
    color: rgba(255, 255, 255, 0.7);
  }

  .hero-section h1 {
    font-size: 3rem;
    font-weight: 700;
    margin: 0 0 1rem;
    background: linear-gradient(135deg, #fff 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: 1.35rem;
    color: rgba(255, 255, 255, 0.9);
    margin: 0 0 0.5rem;
  }

  .subtitle {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.6);
    margin: 0 0 2rem;
  }

  .hero-metrics {
    display: flex;
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .metric {
    text-align: left;
  }

  .metric-value {
    display: block;
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-primary);
  }

  .metric-label {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .hero-ctas {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    padding: 0.875rem 1.75rem;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
  }

  .btn-primary {
    background: linear-gradient(135deg, #8b5cf6, #6366f1);
    color: white;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(139, 92, 246, 0.4);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.15);
  }

  .hero-pricing {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
    padding: 1rem;
    background: rgba(34, 197, 94, 0.1);
    border-radius: 8px;
    border: 1px solid rgba(34, 197, 94, 0.2);
  }

  .pricing-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: #22c55e;
  }

  .pricing-label {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .author-mention {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .author-mention a {
    color: var(--color-primary);
    text-decoration: none;
  }

  /* Hero Visual / Mockup */
  .hero-visual {
    position: relative;
  }

  .hero-mockup {
    background: rgba(15, 15, 35, 0.8);
    border: 1px solid rgba(139, 92, 246, 0.3);
    border-radius: 16px;
    padding: 1.5rem;
    position: relative;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .mockup-badge {
    position: absolute;
    top: -10px;
    right: 20px;
    background: #ef4444;
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.05em;
  }

  .mockup-content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .mockup-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .mockup-nav {
    display: flex;
    gap: 1rem;
  }

  .nav-item {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
    cursor: pointer;
  }

  .nav-item.active {
    color: var(--color-primary);
    font-weight: 600;
  }

  .mockup-live-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.7rem;
    color: #22c55e;
    font-weight: 600;
  }

  .live-dot {
    width: 8px;
    height: 8px;
    background: #22c55e;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .mockup-widgets {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .widget-counter {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .widget-label {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .widget-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: white;
  }

  .widget-trend {
    font-size: 0.75rem;
    font-weight: 600;
  }

  .widget-trend.up {
    color: #22c55e;
  }

  .widget-chart {
    grid-column: span 2;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    padding: 1rem;
  }

  .chart-placeholder {
    display: flex;
    align-items: flex-end;
    gap: 0.5rem;
    height: 80px;
  }

  .chart-bar {
    flex: 1;
    background: rgba(139, 92, 246, 0.3);
    border-radius: 4px 4px 0 0;
    transition: all 0.3s;
  }

  .chart-bar.active {
    background: var(--color-primary);
  }

  .visual-note {
    text-align: center;
    font-size: 0.75rem;
    color: #ef4444;
    margin-top: 0.75rem;
    font-style: italic;
  }

  /* ===== TL;DR SECTION ===== */
  .tldr-section {
    padding: 2rem 0;
    background: rgba(139, 92, 246, 0.05);
    border-top: 1px solid rgba(139, 92, 246, 0.1);
    border-bottom: 1px solid rgba(139, 92, 246, 0.1);
  }

  .tldr-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
  }

  .tldr-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1rem;
  }

  .tldr-icon {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .tldr-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 0.25rem;
  }

  .tldr-value {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.9);
    font-weight: 500;
  }

  .tldr-subvalue {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.5);
    margin-top: 0.25rem;
    font-style: italic;
  }

  /* ===== PROBLEM SECTION ===== */
  .problem-section {
    padding: 5rem 0;
  }

  .problem-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .problem-header h2 {
    font-size: 2rem;
    margin: 0;
  }

  .pain-points {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-bottom: 4rem;
  }

  .pain-point {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: rgba(239, 68, 68, 0.1);
    padding: 1rem 1.5rem;
    border-radius: 12px;
    border: 1px solid rgba(239, 68, 68, 0.2);
  }

  .pain-icon {
    font-size: 1.5rem;
  }

  .pain-content strong {
    display: block;
    color: #ef4444;
    font-size: 1.1rem;
  }

  .pain-content span {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
  }

  /* ===== TRANSFORMATION ===== */
  .transformation-section {
    background: rgba(15, 15, 35, 0.5);
    border-radius: 20px;
    padding: 3rem;
    border: 1px solid rgba(139, 92, 246, 0.1);
  }

  .transformation-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .transformation-header h3 {
    font-size: 1.5rem;
    margin: 0;
  }

  .split-visual {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 2rem;
    align-items: center;
    margin-bottom: 2rem;
  }

  .split-before,
  .split-after {
    text-align: center;
  }

  .split-label {
    display: inline-block;
    background: rgba(255, 255, 255, 0.1);
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    margin-bottom: 1rem;
  }

  .split-label.success {
    background: rgba(34, 197, 94, 0.1);
    color: #22c55e;
  }

  .code-preview {
    background: #1e1e3f;
    border-radius: 12px;
    padding: 1rem;
    text-align: left;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  }

  .code-lines {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
  }

  .code-line {
    padding: 0.2rem 0;
    color: #a5f3fc;
  }

  .code-line.comment {
    color: rgba(255, 255, 255, 0.4);
  }

  .code-line.error {
    color: #ef4444;
  }

  .dashboard-preview {
    background: rgba(15, 15, 35, 0.8);
    border: 1px solid rgba(34, 197, 94, 0.3);
    border-radius: 12px;
    padding: 1rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  }

  .dp-header {
    font-size: 0.8rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    color: white;
  }

  .dp-widgets {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .dp-widget {
    height: 30px;
    background: rgba(139, 92, 246, 0.2);
    border-radius: 4px;
  }

  .dp-widget.large {
    grid-column: span 2;
    height: 40px;
  }

  .dp-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.7rem;
    color: #22c55e;
  }

  .dp-live {
    width: 6px;
    height: 6px;
    background: #22c55e;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  .split-caption {
    display: block;
    margin-top: 0.75rem;
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .split-caption.success {
    color: #22c55e;
  }

  .split-center {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .transform-animation {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .transform-icon {
    font-size: 3rem;
    animation: pulse 2s infinite;
  }

  .transform-text {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .transform-text span:first-child {
    font-weight: 600;
    color: var(--color-primary);
  }

  .transform-time {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .split-features {
    display: flex;
    justify-content: center;
    gap: 2rem;
  }

  .split-feature {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
  }

  .feature-check {
    color: #22c55e;
    font-weight: 700;
  }

  /* ===== DEMO SECTION ===== */
  .demo-section {
    padding: 5rem 0;
  }

  .steps-timeline {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .step {
    flex: 1;
    max-width: 250px;
    background: rgba(15, 15, 35, 0.5);
    border-radius: 16px;
    padding: 2rem;
    text-align: center;
    border: 1px solid rgba(139, 92, 246, 0.1);
    position: relative;
  }

  .step.highlight {
    border-color: var(--color-primary);
    background: rgba(139, 92, 246, 0.1);
  }

  .step-number {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-primary);
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    font-size: 0.75rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .step-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .step h4 {
    margin: 0 0 0.5rem;
    font-size: 1.1rem;
  }

  .step p {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
    margin: 0 0 1rem;
  }

  .step-code,
  .step-ready {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
    font-family: 'JetBrains Mono', monospace;
  }

  .step-tech {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
  }

  .tech-badge {
    background: rgba(139, 92, 246, 0.2);
    color: var(--color-primary);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.7rem;
    font-weight: 600;
  }

  .step-arrow {
    font-size: 1.5rem;
    color: var(--color-primary);
    padding-top: 3rem;
  }

  .code-example {
    background: #1e1e3f;
    border-radius: 12px;
    overflow: hidden;
    max-width: 700px;
    margin: 0 auto;
  }

  .code-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: rgba(0, 0, 0, 0.3);
  }

  .code-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .code-dot.red { background: #ef4444; }
  .code-dot.yellow { background: #eab308; }
  .code-dot.green { background: #22c55e; }

  .code-title {
    margin-left: auto;
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .code-content {
    padding: 1.5rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    color: #a5f3fc;
    margin: 0;
    overflow-x: auto;
  }

  /* ===== USE CASES SECTION ===== */
  .usecases-section {
    padding: 5rem 0;
    background: rgba(139, 92, 246, 0.03);
  }

  .usecases-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .usecase-card {
    background: rgba(15, 15, 35, 0.5);
    border-radius: 16px;
    padding: 1.5rem;
    border: 1px solid rgba(139, 92, 246, 0.1);
    display: flex;
    flex-direction: column;
  }

  .usecase-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .usecase-content h4 {
    margin: 0 0 0.5rem;
    font-size: 1.1rem;
  }

  .usecase-content p {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    margin: 0 0 1rem;
  }

  .usecase-metrics {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 1rem;
  }

  .usecase-metric {
    display: flex;
    flex-direction: column;
  }

  .usecase-metric .metric-value {
    font-size: 1.25rem;
    font-weight: 700;
    color: #22c55e;
  }

  .usecase-metric .metric-label {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .usecase-target {
    margin-top: auto;
    font-size: 0.8rem;
    color: var(--color-primary);
    padding-top: 1rem;
    border-top: 1px solid rgba(139, 92, 246, 0.1);
  }

  /* ===== PROOF SECTION ===== */
  .proof-section {
    padding: 5rem 0;
  }

  .proof-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .proof-item {
    text-align: center;
  }

  .proof-value {
    display: block;
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-primary);
  }

  .proof-label {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .testimonial {
    background: rgba(139, 92, 246, 0.1);
    border-radius: 16px;
    padding: 2rem;
    text-align: center;
    border-left: 4px solid var(--color-primary);
  }

  .testimonial blockquote {
    font-size: 1.1rem;
    font-style: italic;
    margin: 0 0 1rem;
    color: rgba(255, 255, 255, 0.9);
  }

  .testimonial cite {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
  }

  /* ===== MINI CTA FREELANCE ===== */
  .mini-cta-section {
    padding: 2rem 0;
  }

  .mini-cta-banner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(99, 102, 241, 0.1));
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 12px;
    padding: 1rem 2rem;
  }

  .mini-cta-icon {
    font-size: 1.25rem;
  }

  .mini-cta-text {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.95rem;
  }

  .mini-cta-btn {
    background: var(--color-primary);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.85rem;
    transition: all 0.2s;
  }

  .mini-cta-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
  }

  /* ===== TECH SECTION ===== */
  .tech-section {
    padding: 5rem 0;
    background: rgba(15, 15, 35, 0.5);
  }

  .stack-badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.75rem;
    margin-bottom: 3rem;
  }

  .stack-badge {
    background: rgba(139, 92, 246, 0.1);
    color: var(--color-primary);
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.85rem;
    font-weight: 600;
    border: 1px solid rgba(139, 92, 246, 0.2);
  }

  .architecture {
    margin-bottom: 3rem;
  }

  .architecture h3 {
    text-align: center;
    margin-bottom: 1.5rem;
    font-size: 1.25rem;
  }

  .arch-diagram {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }

  .arch-node {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    background: rgba(15, 15, 35, 0.8);
    padding: 1rem 1.5rem;
    border-radius: 12px;
    border: 1px solid rgba(139, 92, 246, 0.2);
    font-size: 0.85rem;
  }

  .arch-node.highlight {
    border-color: var(--color-primary);
    background: rgba(139, 92, 246, 0.1);
  }

  .arch-icon {
    font-size: 1.5rem;
  }

  .arch-arrow {
    color: var(--color-primary);
    font-size: 1.25rem;
  }

  .arch-sub {
    display: flex;
    justify-content: center;
  }

  .arch-sub-flow {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: rgba(255, 255, 255, 0.03);
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-size: 0.8rem;
  }

  .arch-sub-node {
    color: rgba(255, 255, 255, 0.6);
  }

  .arch-sub-arrow {
    color: var(--color-primary);
    font-size: 0.9rem;
  }

  .tech-decisions {
    margin-bottom: 3rem;
  }

  .tech-decisions h3 {
    text-align: center;
    margin-bottom: 1.5rem;
    font-size: 1.25rem;
  }

  .decisions-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .decision {
    background: rgba(15, 15, 35, 0.5);
    padding: 1.5rem;
    border-radius: 12px;
    border: 1px solid rgba(139, 92, 246, 0.1);
  }

  .decision h4 {
    margin: 0 0 0.5rem;
    font-size: 1rem;
    color: var(--color-primary);
  }

  .decision p {
    margin: 0;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .challenges h3 {
    text-align: center;
    margin-bottom: 1.5rem;
    font-size: 1.25rem;
  }

  .challenges-table {
    max-width: 800px;
    margin: 0 auto;
  }

  .challenge-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .challenge,
  .solution {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem;
    border-radius: 8px;
    font-size: 0.85rem;
  }

  .challenge {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.2);
  }

  .challenge-icon {
    color: #ef4444;
  }

  .solution {
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.2);
  }

  .solution-icon {
    color: #22c55e;
    font-weight: 700;
  }

  /* ===== ROLE SECTION ===== */
  .role-section {
    padding: 5rem 0;
  }

  .role-intro {
    display: flex;
    justify-content: center;
    margin-bottom: 2rem;
  }

  .role-badge-box {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: rgba(139, 92, 246, 0.1);
    padding: 1rem 1.5rem;
    border-radius: 12px;
  }

  .rb-icon {
    font-size: 1.5rem;
  }

  .rb-info {
    display: flex;
    flex-direction: column;
  }

  .rb-info strong {
    font-size: 1rem;
  }

  .rb-info span {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .skills-columns {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .skill-column {
    background: rgba(255, 255, 255, 0.03);
    border-radius: 12px;
    padding: 1.25rem;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .sc-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .sc-icon {
    font-size: 1.25rem;
  }

  .sc-header h4 {
    margin: 0;
    font-size: 0.95rem;
  }

  .sc-list {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .sc-list li {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.7);
    padding: 0.4rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.03);
  }

  .sc-list li:last-child {
    border-bottom: none;
  }

  .methodology-bar {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem;
    background: rgba(139, 92, 246, 0.05);
    border-radius: 8px;
    font-size: 0.85rem;
  }

  .mb-label {
    color: rgba(255, 255, 255, 0.5);
  }

  .mb-item {
    color: var(--color-primary);
    font-weight: 500;
  }

  .mb-sep {
    color: rgba(255, 255, 255, 0.2);
  }

  /* ===== CTA SECTION ===== */
  .cta-section {
    padding: 5rem 0;
    background: linear-gradient(180deg, rgba(139, 92, 246, 0.1) 0%, transparent 100%);
  }

  .dual-cta {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 3rem;
    align-items: center;
  }

  .cta-product,
  .cta-freelance {
    background: rgba(15, 15, 35, 0.5);
    border-radius: 20px;
    padding: 2.5rem;
    text-align: center;
    border: 1px solid rgba(139, 92, 246, 0.1);
  }

  .cta-product h3,
  .cta-freelance h3 {
    margin: 0 0 0.75rem;
    font-size: 1.5rem;
  }

  .cta-product p,
  .cta-freelance p {
    color: rgba(255, 255, 255, 0.6);
    margin: 0 0 1.5rem;
  }

  .cta-price {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .price-value {
    font-size: 2.5rem;
    font-weight: 700;
    color: #22c55e;
  }

  .price-period {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .cta-specialization {
    background: rgba(139, 92, 246, 0.1);
    color: var(--color-primary);
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.85rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
    display: inline-block;
  }

  .btn-link {
    display: block;
    margin-top: 1rem;
    color: var(--color-primary);
    text-decoration: none;
    font-size: 0.9rem;
  }

  .cta-subtext {
    margin-top: 1rem !important;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.5) !important;
  }

  .cta-divider {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .cta-divider span {
    background: rgba(139, 92, 246, 0.2);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 1024px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero-content {
      text-align: center;
    }

    .hero-metrics {
      justify-content: center;
    }

    .hero-ctas {
      justify-content: center;
    }

    .hero-pricing {
      justify-content: center;
    }

    .tldr-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .usecases-grid {
      grid-template-columns: 1fr;
    }

    .proof-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .decisions-grid {
      grid-template-columns: 1fr;
    }

    .skills-columns {
      grid-template-columns: repeat(2, 1fr);
    }

    .dual-cta {
      grid-template-columns: 1fr;
    }

    .cta-divider {
      padding: 1rem 0;
    }
  }

  @media (max-width: 768px) {
    .hero-section h1 {
      font-size: 2.5rem;
    }

    .hero-metrics {
      flex-direction: column;
      gap: 1.5rem;
    }

    .tldr-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .pain-points {
      flex-direction: column;
      gap: 1rem;
    }

    .split-visual {
      grid-template-columns: 1fr;
    }

    .split-center {
      flex-direction: row;
      padding: 1rem 0;
    }

    .steps-timeline {
      flex-direction: column;
    }

    .step-arrow {
      transform: rotate(90deg);
      padding: 0;
    }

    .arch-diagram {
      flex-direction: column;
    }

    .arch-arrow {
      transform: rotate(90deg);
    }

    .skills-columns {
      grid-template-columns: 1fr;
    }

    .mini-cta-banner {
      flex-direction: column;
      text-align: center;
    }

    .challenge-row {
      grid-template-columns: 1fr;
    }
  }
</style>
