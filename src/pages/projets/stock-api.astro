---
import Layout from '../../layouts/Layout.astro';
import { Image } from 'astro:assets';
import ProjectNavigation from '../../components/ProjectNavigation.astro';

// Import des images - à décommenter quand les images seront ajoutées
// import swaggerImg from '../../assets/images/projets/stock-api/swagger.png';
// import productsImg from '../../assets/images/projets/stock-api/products.png';
// import movementsImg from '../../assets/images/projets/stock-api/movements.png';

// Configuration des screenshots
const screenshots = [
  {
    // image: swaggerImg,  // Décommenter quand l'image existe
    placeholder: { icon: '📚', text: 'Documentation API' },
    caption: "Swagger UI interactive"
  },
  {
    // image: productsImg,
    placeholder: { icon: '📦', text: 'Liste produits' },
    caption: 'Endpoint GET /api/products'
  },
  {
    // image: movementsImg,
    placeholder: { icon: '📉', text: 'Mouvements stock' },
    caption: 'Historique des entrées/sorties'
  }
];
---

<Layout title="Stock API — REST API Gestion de Stock | Audrey Tesson" description="API de gestion de stock multi-entrepôts. Vision temps réel, traçabilité, ruptures évitées." activePage="projets">
  <!-- Project Header -->
  <section class="project-detail-header">
    <div class="container">
      <a href="/projets" class="back-link">← Retour aux projets</a>
      <div class="project-meta">
        <span class="project-type-badge">API REST</span>
        <span class="project-status-personal">Projet personnel</span>
      </div>
      <h1>Stock API</h1>
      <p class="project-subtitle">API de gestion de stock. Vision temps réel, traçabilité complète, ruptures évitées.</p>
    </div>
  </section>

  <!-- Project Content -->
  <section class="project-content-section">
    <div class="container">
      <div class="content-grid">
        <div class="content-main">
          <div class="content-section">
            <h2>Le problème</h2>
            <p>Gérer les stocks sans API dédiée implique des tableurs, des données dispersées et des <strong>ruptures découvertes trop tard</strong>. Impossible d'avoir une vision consolidée ni des alertes automatiques.</p>

            <div class="comparison-block">
              <div class="compare-box before">
                <div class="compare-header">
                  <span>❌</span> Sans API stock
                </div>
                <ul class="compare-list">
                  <li>Données dispersées (Excel, papier)</li>
                  <li>Ruptures découvertes trop tard</li>
                  <li>Aucune traçabilité des mouvements</li>
                  <li>Intégration ERP impossible</li>
                </ul>
              </div>
              <div class="arrow-divider">→</div>
              <div class="compare-box after">
                <div class="compare-header">
                  <span>✅</span> Avec Stock API
                </div>
                <ul class="compare-list">
                  <li>Vision centralisée en temps réel</li>
                  <li>Alertes stock bas automatiques</li>
                  <li>Historique complet des mouvements</li>
                  <li>Intégration facile (REST)</li>
                </ul>
              </div>
            </div>
          </div>

          <div class="content-section">
            <h2>La solution</h2>
            <p>Une API REST complète pour gérer <strong>produits</strong>, <strong>mouvements</strong> et <strong>alertes</strong>. Documentation OpenAPI auto-générée, filtres avancés et historique complet.</p>
            <p class="project-objective">→ Résultat : vision temps réel, ruptures évitées, traçabilité des mouvements.</p>
          </div>

          <div class="content-section">
            <h2>Aperçu</h2>
            <div class="screenshots-grid">
              {screenshots.map((screenshot) => (
                <figure class="screenshot">
                  {screenshot.image ? (
                    <Image
                      src={screenshot.image}
                      alt={screenshot.caption}
                      class="screenshot-image"
                      loading="lazy"
                    />
                  ) : (
                    <div class="screenshot-placeholder">
                      <span>{screenshot.placeholder.icon}</span>
                      <p>{screenshot.placeholder.text}</p>
                    </div>
                  )}
                  <figcaption>{screenshot.caption}</figcaption>
                </figure>
              ))}
            </div>
          </div>

          <div class="content-section">
            <h2>Endpoints principaux</h2>
            <div class="endpoints-list">
              <div class="endpoint">
                <span class="method get">GET</span>
                <code>/api/products</code>
                <span class="endpoint-desc">Liste des produits (paginée, filtrable)</span>
              </div>
              <div class="endpoint">
                <span class="method post">POST</span>
                <code>/api/products</code>
                <span class="endpoint-desc">Créer un nouveau produit</span>
              </div>
              <div class="endpoint">
                <span class="method get">GET</span>
                <code>/api/stock-movements</code>
                <span class="endpoint-desc">Historique des mouvements</span>
              </div>
              <div class="endpoint">
                <span class="method post">POST</span>
                <code>/api/stock-movements</code>
                <span class="endpoint-desc">Enregistrer un mouvement</span>
              </div>
              <div class="endpoint">
                <span class="method get">GET</span>
                <code>/api/reports/low-stock</code>
                <span class="endpoint-desc">Produits en stock bas</span>
              </div>
            </div>
          </div>

          <div class="content-section">
            <h2>Fonctionnalités</h2>
            <div class="features-list">
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>CRUD complet</strong>
                  <span>Produits, catégories, fournisseurs</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Mouvements de stock</strong>
                  <span>Entrées, sorties, ajustements avec traçabilité</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Filtres avancés</strong>
                  <span>Par catégorie, stock, date, recherche texte</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Documentation OpenAPI</strong>
                  <span>Swagger UI intégré et export JSON</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <aside class="content-sidebar">
          <div class="sidebar-card sidebar-stack">
            <h3>Stack</h3>
            <div class="stack-badges">
              <span class="stack-badge backend">PHP 8.4</span>
              <span class="stack-badge backend">Symfony 7.2</span>
              <span class="stack-badge backend">API Platform 4</span>
              <span class="stack-badge db">PostgreSQL</span>
              <span class="stack-badge ai">OpenAPI 3.0</span>
              <span class="stack-badge infra">Docker</span>
              <span class="stack-badge infra">GitHub</span>
              <span class="stack-badge quality">PHPStan (lvl 8)</span>
              <span class="stack-badge quality">PHPUnit</span>
              <span class="stack-badge quality">Playwright</span>
            </div>
          </div>

          <div class="sidebar-card sidebar-code">
            <h3>Code source</h3>
            <a href="https://github.com/audrey-tech/stock-api" target="_blank" class="code-badge public">
              <span class="code-icon">📂</span>
              <span>Voir sur GitHub</span>
            </a>
          </div>

          <div class="sidebar-card cta-card-sidebar">
            <h3>Besoin d'une API stock ?</h3>
            <p>Je peux développer une API sur mesure pour gérer vos stocks.</p>
            <a href="/contact" class="btn btn-primary btn-small">Parlons-en</a>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <ProjectNavigation currentSlug="stock-api" />
</Layout>

<style>
  .project-status-demo {
    background: rgba(96, 165, 250, 0.15);
    color: var(--accent-primary);
    padding: 0.4rem 1rem;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 500;
  }

  .endpoints-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .endpoint {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.75rem 1rem;
    background: var(--surface-light);
    border: 1px solid var(--border);
    border-radius: 8px;
    flex-wrap: wrap;
  }

  .method {
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
  }

  .method.get {
    background: rgba(34, 197, 94, 0.15);
    color: #22c55e;
  }

  .method.post {
    background: rgba(59, 130, 246, 0.15);
    color: #3b82f6;
  }

  .endpoint code {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    color: var(--text-primary);
  }

  .endpoint-desc {
    color: var(--text-muted);
    font-size: 0.85rem;
    margin-left: auto;
  }

  @media (max-width: 768px) {
    .endpoint {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }
    .endpoint-desc {
      margin-left: 0;
    }
  }
</style>
