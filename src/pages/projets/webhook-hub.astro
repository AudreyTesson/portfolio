---
import Layout from '../../layouts/Layout.astro';
import { Image } from 'astro:assets';
import ProjectNavigation from '../../components/ProjectNavigation.astro';

// Configuration des screenshots
const screenshots = [
  {
    placeholder: { icon: '🔗', text: 'Webhook entrant' },
    caption: 'Réception et validation des webhooks'
  },
  {
    placeholder: { icon: '📊', text: 'Dashboard' },
    caption: 'Monitoring et statistiques'
  },
  {
    placeholder: { icon: '🔄', text: 'Retry' },
    caption: 'Système de retry automatique'
  }
];
---

<Layout title="Webhook Hub — API d'intégration webhooks | Audrey Tesson" description="Hub centralisé pour vos webhooks Stripe, GitHub, Slack. Fiabilité garantie, zéro webhook perdu." activePage="projets">
  <!-- Project Header -->
  <section class="project-detail-header">
    <div class="container">
      <a href="/projets" class="back-link">← Retour aux projets</a>
      <div class="project-meta">
        <span class="project-type-badge">API Backend</span>
        <span class="project-status-personal">Projet personnel</span>
      </div>
      <h1>Webhook Hub</h1>
      <p class="project-subtitle">Hub centralisé pour vos intégrations webhook. Fiabilité garantie, zéro événement perdu.</p>
    </div>
  </section>

  <!-- Project Content -->
  <section class="project-content-section">
    <div class="container">
      <div class="content-grid">
        <div class="content-main">
          <div class="content-section">
            <h2>Le problème</h2>
            <p>Gérer les webhooks de multiples services (Stripe, GitHub, Slack...) devient vite ingérable. Chaque provider a son propre format de signature, les <strong>webhooks échouent silencieusement</strong>, et sans logs centralisés, le debugging est un cauchemar.</p>

            <div class="comparison-block">
              <div class="compare-box before">
                <div class="compare-header">
                  <span>❌</span> Sans hub centralisé
                </div>
                <ul class="compare-list">
                  <li>Webhooks perdus silencieusement</li>
                  <li>Debugging impossible</li>
                  <li>Validation manuelle par provider</li>
                  <li>Pas de retry en cas d'échec</li>
                </ul>
              </div>
              <div class="arrow-divider">→</div>
              <div class="compare-box after">
                <div class="compare-header">
                  <span>✅</span> Avec Webhook Hub
                </div>
                <ul class="compare-list">
                  <li>Zéro webhook perdu</li>
                  <li>Logs centralisés et traçabilité</li>
                  <li>Validation cryptographique automatique</li>
                  <li>Retry automatique intelligent</li>
                </ul>
              </div>
            </div>
          </div>

          <div class="content-section">
            <h2>La solution</h2>
            <p>Un hub centralisé qui <strong>reçoit</strong>, <strong>valide</strong> et <strong>dispatche</strong> vos webhooks vers vos applications. Validation cryptographique par provider, logs complets, retry automatique en cas d'échec.</p>
            <p class="project-objective">→ Résultat : fiabilité des intégrations, traçabilité totale, zéro événement perdu.</p>
          </div>

          <div class="content-section">
            <h2>Fonctionnalités</h2>
            <div class="features-list">
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Validation des signatures</strong>
                  <span>Vérification cryptographique HMAC-SHA256 par provider</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Multi-provider</strong>
                  <span>Support Stripe, GitHub, Slack, extensible à d'autres</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Logging complet</strong>
                  <span>Historique de tous les webhooks avec payload et headers</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Système de retry</strong>
                  <span>Retry automatique avec backoff exponentiel (1min, 5min, 15min)</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Forward HTTP</strong>
                  <span>Dispatch des webhooks vers vos applications internes</span>
                </div>
              </div>
              <div class="feature-item">
                <span class="feature-check">✓</span>
                <div class="feature-text">
                  <strong>Dashboard monitoring</strong>
                  <span>Stats temps réel, health check et taux d'erreurs</span>
                </div>
              </div>
            </div>
          </div>

          <div class="content-section">
            <h2>Aperçu</h2>
            <div class="screenshots-grid">
              {screenshots.map((screenshot) => (
                <figure class="screenshot">
                  {screenshot.image ? (
                    <Image
                      src={screenshot.image}
                      alt={screenshot.caption}
                      class="screenshot-image"
                      loading="lazy"
                    />
                  ) : (
                    <div class="screenshot-placeholder">
                      <span>{screenshot.placeholder.icon}</span>
                      <p>{screenshot.placeholder.text}</p>
                    </div>
                  )}
                  <figcaption>{screenshot.caption}</figcaption>
                </figure>
              ))}
            </div>
          </div>

          <div class="content-section">
            <h2>Providers supportés</h2>
            <div class="challenges-grid">
              <div class="challenge-card">
                <span class="challenge-icon">💳</span>
                <h4>Stripe</h4>
                <p>Validation via header Stripe-Signature avec timestamp anti-replay</p>
              </div>
              <div class="challenge-card">
                <span class="challenge-icon">🐙</span>
                <h4>GitHub</h4>
                <p>Signature X-Hub-Signature-256 pour webhooks repository</p>
              </div>
              <div class="challenge-card">
                <span class="challenge-icon">💬</span>
                <h4>Slack</h4>
                <p>Validation X-Slack-Signature avec request timestamp</p>
              </div>
              <div class="challenge-card">
                <span class="challenge-icon">➕</span>
                <h4>Extensible</h4>
                <p>Architecture modulaire pour ajouter de nouveaux providers</p>
              </div>
            </div>
          </div>

          <div class="content-section">
            <h2>Architecture</h2>
            <div class="architecture-grid">
              <div class="architecture-card">
                <h4>Signature Verifiers</h4>
                <p>Pattern Strategy avec tagged iterators pour la validation des signatures par provider</p>
              </div>
              <div class="architecture-card">
                <h4>Messenger + RabbitMQ</h4>
                <p>Traitement asynchrone des webhooks pour une réponse rapide aux providers</p>
              </div>
              <div class="architecture-card">
                <h4>Retry avec backoff</h4>
                <p>3 tentatives automatiques avec délais progressifs en cas d'échec du forward</p>
              </div>
            </div>
          </div>
        </div>

        <aside class="content-sidebar">
          <div class="sidebar-card sidebar-stack">
            <h3>Stack</h3>
            <div class="stack-badges">
              <span class="stack-badge backend">PHP 8.3</span>
              <span class="stack-badge backend">Symfony 7.4</span>
              <span class="stack-badge backend">API Platform 4</span>
              <span class="stack-badge db">MySQL 8.0</span>
              <span class="stack-badge db">Redis</span>
              <span class="stack-badge infra">RabbitMQ</span>
              <span class="stack-badge infra">Docker</span>
              <span class="stack-badge quality">PHPStan (lvl 8)</span>
              <span class="stack-badge quality">PHPUnit</span>
            </div>
          </div>

          <div class="sidebar-card sidebar-code">
            <h3>Code source</h3>
            <a href="https://github.com/audrey-tech/webhook-hub" target="_blank" class="code-badge public">
              <span class="code-icon">📂</span>
              <span>Voir sur GitHub</span>
            </a>
          </div>

          <div class="sidebar-card api-endpoints">
            <h3>Endpoints API</h3>
            <div class="endpoint-list">
              <code>POST /webhook/stripe</code>
              <code>POST /webhook/github</code>
              <code>POST /webhook/slack</code>
              <code>GET /api/webhook_logs</code>
              <code>GET /api/dashboard/stats</code>
              <code>GET /api/dashboard/health</code>
            </div>
          </div>

          <div class="sidebar-card cta-card-sidebar">
            <h3>Besoin d'intégrations fiables ?</h3>
            <p>Je peux centraliser vos webhooks et garantir leur traitement.</p>
            <a href="/contact" class="btn btn-primary btn-small">Parlons-en</a>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <ProjectNavigation currentSlug="webhook-hub" />
</Layout>

<style>
  .architecture-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .architecture-card {
    padding: 1.25rem;
    background: var(--surface-light);
    border: 1px solid var(--border);
    border-radius: 12px;
    border-top: 3px solid var(--accent-primary);
  }

  .architecture-card h4 {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
  }

  .architecture-card p {
    color: var(--text-secondary);
    font-size: 0.8rem;
    line-height: 1.4;
  }

  @media (max-width: 768px) {
    .architecture-grid {
      grid-template-columns: 1fr;
    }
  }

  .api-endpoints {
    background: var(--surface-light);
  }

  .endpoint-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .endpoint-list code {
    display: block;
    padding: 0.5rem 0.75rem;
    background: var(--surface-dark);
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    color: var(--accent-primary);
    font-family: 'JetBrains Mono', monospace;
  }
</style>
